# Tasks

- [x] Task 1: Fix 15-minute task drag in `beginBlockDrag` — Fix `offsetMinutes` calculation to handle minimum-duration tasks correctly
- [x] Task 2: Fix 15-minute task title display — Adjust CSS in `src/styles.css` to ensure short-block titles are visible
- [x] Task 3: Fix candidate drop alignment — Add `startHour: dayStartHour` to `getDropTargetFromPointer` call in `beginShelfDrag`
- [x] Task 4: Build & verify — Run `npm run build` and fix any errors

# Task Dependencies
- Tasks 1-3 are independent
- Task 4 depends on all prior tasks
